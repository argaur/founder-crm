// Copy this file to config.js and fill in your values.
// config.js is gitignored — never commit it.

const AIRTABLE_PAT     = 'YOUR_PAT_HERE';       // Read-only PAT scoped to data.records:read
const AIRTABLE_BASE_ID = 'YOUR_BASE_ID_HERE';   // Starts with "app..."
const USER_ID          = 'demo-gaurav-001';     // The user_id from /register
const BOT_LINK         = 'https://t.me/RethinkCRMbot';
const REFRESH_MS       = 5 * 60 * 1000;         // Auto-refresh every 5 minutes
